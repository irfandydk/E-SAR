<?php 
// File: sarsip/login.php
session_start();
include 'config/koneksi.php';

// Ambil Nama Aplikasi dari DB
$app_name_display = "E-ARSIP";
$q_setting = @mysqli_query($koneksi, "SELECT nama_aplikasi, logo_path FROM app_settings LIMIT 1");
if($q_setting && mysqli_num_rows($q_setting) > 0){
    $d = mysqli_fetch_assoc($q_setting);
    if(!empty($d['nama_aplikasi'])) $app_name_display = $d['nama_aplikasi'];
}
?>

<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - <?php echo $app_name_display; ?></title>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    
    <script>
        const storedTheme = localStorage.getItem('theme') || 'light';
        document.documentElement.setAttribute('data-bs-theme', storedTheme);
    </script>

    <style>
        body {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            transition: background 0.3s;
            overflow: hidden; /* Mencegah scrollbar saat animasi */
        }

        /* Override Background untuk Dark Mode */
        [data-bs-theme="dark"] body {
            background: linear-gradient(135deg, #0f2027 0%, #203a43 50%, #2c5364 100%);
        }

        /* --- 1. KEYFRAMES ANIMASI (POJOK KIRI ATAS -> TENGAH) --- */
        @keyframes swoopIn {
            0% {
                opacity: 0;
                transform: translate(-100vw, -100vh) scale(0.1) rotate(-10deg);
            }
            60% {
                transform: translate(20px, 20px) scale(1.05) rotate(2deg); /* Efek membal sedikit */
            }
            100% {
                opacity: 1;
                transform: translate(0, 0) scale(1) rotate(0deg);
            }
        }

        .login-card {
            width: 100%;
            max-width: 400px;
            border: none;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            
            /* Terapkan Animasi */
            animation: swoopIn 1.2s cubic-bezier(0.22, 1, 0.36, 1) forwards;
            
            /* Glassmorphism Effect */
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
        }

        [data-bs-theme="dark"] .login-card {
            background: rgba(33, 37, 41, 0.9);
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }

        .logo-login {
            width: 70px;
            height: 70px;
            object-fit: contain;
            background: #fff;
            padding: 5px;
            border-radius: 50%;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
            margin-bottom: 15px;
        }

        .form-floating:focus-within { z-index: 2; }
    </style>
</head>
<body>

    <div class="container d-flex justify-content-center">
        
        <div class="card login-card p-4">
            <div class="card-body text-center">
                
                <?php if(!empty($d['logo_path']) && file_exists('assets/'.$d['logo_path'])): ?>
                    <img src="assets/<?php echo $d['logo_path']; ?>" alt="Logo" class="logo-login">
                <?php else: ?>
                    <div class="d-inline-flex align-items-center justify-content-center bg-primary text-white rounded-circle mb-3 shadow" style="width: 60px; height: 60px;">
                        <i class="bi bi-building fs-2"></i>
                    </div>
                <?php endif; ?>

                <h4 class="fw-bold mb-1"><?php echo $app_name_display; ?></h4>
                <p class="text-muted small mb-4">Silakan login untuk mengakses arsip.</p>

                <?php if(isset($_GET['pesan'])){ ?>
                    <?php if($_GET['pesan'] == "gagal"){ ?>
                        <div class="alert alert-danger py-2 small" role="alert"><i class="bi bi-x-circle me-1"></i> Login gagal! Username/Password salah.</div>
                    <?php } else if($_GET['pesan'] == "logout"){ ?>
                        <div class="alert alert-success py-2 small" role="alert"><i class="bi bi-check-circle me-1"></i> Anda telah berhasil logout.</div>
                    <?php } else if($_GET['pesan'] == "belum_login"){ ?>
                        <div class="alert alert-warning py-2 small" role="alert"><i class="bi bi-exclamation-triangle me-1"></i> Anda harus login terlebih dahulu.</div>
                    <?php } ?>
                <?php } ?>

                <form action="cek_login.php" method="POST" autocomplete="off">
                    
                    <div class="form-floating mb-3 text-start">
                        <input type="text" class="form-control" id="username" name="username" placeholder="Username" required>
                        <label for="username"><i class="bi bi-person me-2"></i>Username</label>
                    </div>

                    <div class="form-floating mb-4 text-start">
                        <input type="password" class="form-control" id="password" name="password" placeholder="Password" required>
                        <label for="password"><i class="bi bi-key me-2"></i>Password</label>
                        <div class="position-absolute end-0 top-50 translate-middle-y me-3" style="cursor: pointer; z-index: 5;" onclick="togglePassword()">
                            <i class="bi bi-eye-slash" id="eyeIcon"></i>
                        </div>
                    </div>

                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-primary btn-lg fw-bold shadow-sm">
                            MASUK <i class="bi bi-box-arrow-in-right ms-2"></i>
                        </button>
                    </div>

                </form>
                
                <div class="mt-4 pt-3 border-top">
                    <small class="text-muted opacity-75">&copy; <?php echo date('Y'); ?> <?php echo $app_name_display; ?> v2.0</small>
                </div>

            </div>
        </div>

    </div>

    <script>
        function togglePassword() {
            var passInput = document.getElementById("password");
            var icon = document.getElementById("eyeIcon");
            if (passInput.type === "password") {
                passInput.type = "text";
                icon.classList.remove("bi-eye-slash");
                icon.classList.add("bi-eye");
            } else {
                passInput.type = "password";
                icon.classList.remove("bi-eye");
                icon.classList.add("bi-eye-slash");
            }
        }
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>